spring.application.name=flexistock.inventory
server.port=8082

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/inventory_sql}
spring.datasource.username=${DB_USERNAME:mithilareddygaddam}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/inventory_nosql}

users.service.base-url=${USERS_SERVICE_BASE_URL:http://localhost:8081}

spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:${MAIL_USERNAME:mithila1705d6@gmail.com}}
spring.mail.password=${SPRING_MAIL_PASSWORD:${MAIL_APP_PASSWORD:${MAIL_PASSWORD:wwuqgyqjdxepzrod}}}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_SMTP_STARTTLS_ENABLE:true}
spring.mail.properties.mail.smtp.starttls.required=${SPRING_MAIL_SMTP_STARTTLS_REQUIRED:true}
spring.mail.properties.mail.smtp.connectiontimeout=${SPRING_MAIL_SMTP_CONNECTION_TIMEOUT:5000}
spring.mail.properties.mail.smtp.timeout=${SPRING_MAIL_SMTP_TIMEOUT:5000}
spring.mail.properties.mail.smtp.writetimeout=${SPRING_MAIL_SMTP_WRITE_TIMEOUT:5000}

inventory.notifications.low-stock.enabled=${INVENTORY_LOW_STOCK_NOTIFICATIONS_ENABLED:true}
inventory.notifications.from-address=${INVENTORY_NOTIFICATIONS_FROM_ADDRESS:${spring.mail.username:no-reply@flexistock.local}}
inventory.notifications.low-stock.admin-emails=${INVENTORY_LOW_STOCK_ADMIN_EMAILS:}
